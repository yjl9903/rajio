---
title: 检查运行环境
description: 检查 Rajio 版本、Node.js、.env、服务商、本地工具和 Codex。
---

# 检查运行环境

```bash
rajio doctor <target>
```

## 用途

`doctor` 检查当前会话运行 Rajio 所需的环境是否可用，包括 CLI 版本、Node.js、`.env`、转写服务商、OpenAI、ffmpeg、ffprobe 和 Codex。

## 典型用法

```bash
rajio doctor /path/to/session
rajio doctor /path/to/session.toml
```

## 参数

| 参数       | 说明                                             |
| ---------- | ------------------------------------------------ |
| `<target>` | 必填。已有会话工作区或可解析到会话工作区的路径。 |

`doctor` 没有额外 CLI 选项。

## 产物或副作用

- 不修改会话文件。
- 会请求 npm registry 检查 Rajio 最新版本。
- 会读取当前工作目录 `.env` 和会话工作区 `.env`。
- 会按会话的 `[transcription]` 配置探测转写服务商：ElevenLabs 或 OpenAI-compatible API。
- 如果设置了 `OPENAI_API_KEY`，还会探测人工 AI 阶段使用的 OpenAI API。
- 会运行 `ffmpeg -version` 和 `ffprobe -version`。
- 如果设置了 `OPENAI_API_KEY`，会初始化 `@openai/codex-sdk`。
- 任一 `fail` 检查会让退出码变为 `1`。

## 常见 Agent 调用方式

```bash
rajio doctor /path/to/session
```

Agent 通常在自动转写前运行一次，或在用户的 `.env`、ffmpeg、Node.js、服务商配置看起来异常时运行。

## 注意事项

- Node.js 要求 `>=24`。
- 使用 ElevenLabs 转写时，`ELEVENLABS_API_KEY` 缺失是失败。
- 使用 OpenAI 转写时，`OPENAI_API_KEY` 缺失是失败。
- 没有选择 OpenAI 转写时，`OPENAI_API_KEY` 缺失是 warning，代表人工 AI 阶段相关能力不可用。
- 网络不可用时，版本检查或服务商检查可能 warning/fail；这不代表本地文件一定损坏。
