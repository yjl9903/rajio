---
title: 快速开始
description: 创建一个 Rajio 转写和翻译会话。
---

# 快速开始

先在 workspace 根目录安装依赖并构建 CLI，然后对媒体文件或已有会话目录运行 Rajio。

```bash
pnpm install
pnpm --filter rajio build
rajio ./path/to/session
```

Rajio 会把工作流状态写入 `session.toml`，并把可编辑的转写、翻译和字幕产物保存在会话目录中。
